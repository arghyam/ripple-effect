export declare enum Type {
    Constant = 0,
    Instance = 1,
    Factory = 2
}
export declare enum Scope {
    Container = 0,
    Resolution = 1,
    Singleton = 2,
    Transient = 3
}
export interface Binding {
    readonly impl: unknown;
    readonly type: Type;
    clone?: () => Binding;
}
